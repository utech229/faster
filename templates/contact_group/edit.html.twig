<!DOCTYPE html>

<title>Edit ContactGroup</title>

{% block body %}
    <h1>Edit ContactGroup</h1>

    {{ include('contact_group/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_contact_group_index') }}">back to list</a>

    {{ include('contact_group/_delete_form.html.twig') }}
{% endblock %}
