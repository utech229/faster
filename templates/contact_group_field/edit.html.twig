<!DOCTYPE html>

<title>Edit ContactGroupField</title>

{% block body %}
    <h1>Edit ContactGroupField</h1>

    {{ include('contact_group_field/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_contact_group_field_index') }}">back to list</a>

    {{ include('contact_group_field/_delete_form.html.twig') }}
{% endblock %}
